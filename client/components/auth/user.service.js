'use strict';

angular.module('researchApp')
  .factory('User', function ($resource) {
    return $resource(API_URL + 'users/:id/:controller', {
      id: '@_id'
    },
    {
      changePassword: {
        method: 'PUT',
        params: {
          controller:'password'
        }
      },
      get: {
        method: 'GET',
        params: {
          id:'me'
        }
      }
	  });
  });
