'use strict';

define(['angular'], function (angular) {

    angular.module('researchApp.Services')
        .factory('ResearchesService', ['User',
        function (User) {
          return {};
    }]);
});
